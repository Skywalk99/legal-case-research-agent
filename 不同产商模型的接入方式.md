# Legal Case Research Agent：模型提供商接入方式

项目使用 `langchain-dev-utils` 的 `load_chat_model` 按 `provider:model` 格式加载模型。`src/__init__.py` 注册额外提供商，`src/agent/utils/context.py` 的 `Context` 选择三个 Agent 阶段所用模型。

## 当前默认模型

```text
plan_model    = moonshot:kimi-k2.6
sub_model     = deepseek:deepseek-v4-flash
summary_model = dashscope:qwen-flash
```

- `plan_model`：Main Agent 的计划、委派与最终回答。
- `sub_model`：SubAgent 的案例研究和工具选择。
- `summary_model`：将 SubAgent 写入内容整理为返回 Main Agent 的工具摘要。

模型名称是否在具体提供商账户中可用，取决于提供商当前服务与权限；仓库代码只声明上述默认字符串。

## 已注册或依赖支持的提供商

- `dashscope`：通过 `ChatQwen` 注册。
- `siliconflow`：通过 `ChatSiliconFlow` 注册。
- `zai`：注册为 OpenAI-compatible，固定基址为智谱开放平台。
- `moonshot`：注册为 OpenAI-compatible，固定基址为 Moonshot API。
- `deepseek`：由项目依赖 `langchain-deepseek` 提供。

切换模型时，修改 `src/agent/utils/context.py` 中相应 `Context` 字段，并配置该提供商所需环境变量。不要把真实密钥写入源码、Markdown 或前端 `VITE_` 环境变量。

常用变量名见根目录 [`.env.example`](./.env.example)。法律案例工具还单独需要 `DELI_API_KEY`，它与模型提供商无关。
